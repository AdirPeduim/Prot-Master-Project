import React, {Fragment, FC } from "react";
import {
  TextField,
  withStyles,
  Button,
  Grid,
  Card,
  CardMedia,
} from "@material-ui/core";

interface MyProps {
  classes: any;
}
const styles = {
  title: {
    fontSize: "50px",
  },
};

const Page2: FC<MyProps> = ({ classes }) => {
    

  
    return (
    <Fragment>
      
      
    </Fragment>
  );
};

export default withStyles(styles)(Page2);
